## LLM provider swap

gemini-2.0-flash hit free-tier quota mid-eval (RESOURCE_EXHAUSTED).
gemini-2.0-flash-lite did the same within an hour. burned the daily limit on
about 80 /investigate calls.

swapping to OpenRouter -> meta-llama/llama-3.1-8b-instruct (free tier).
slower (~3-5s vs ~1s) but stable for capstone scope. provider abstracted in
narrative.py so we can swap back later.
